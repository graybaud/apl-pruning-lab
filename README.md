# apl-pruning-lab
Minimal APL-like DSL for describing LLM pruning methods. Compiles to JAX.  Test 10 pruning variants in 10 minutes instead of 2 days.  LLM-friendly: give it the primitives, it writes the formulas.
