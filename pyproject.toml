[project]
name = "apl-pruning"
version = "0.2.0"
description = "Minimal APL-like DSL for describing LLM pruning methods. Compiles to numpy."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
